div(ng-controller="mvNavBarLoginController")
  form.form-inline(ng-hide="identity.isAuthenticated()")
    input.form-control(placeholder="username", ng-model="username")
    input.form-control(type="password", placeholder="password", ng-model="password")
    button.btn.btn-primary(ng-click="signin(username, password)") Sign In
  ul.navbar-nav(ng-show="identity.isAuthenticated()")
    li.nav-item.active.dropdown
      a.nav-link.dropdown-toggle(href="", data-toggle="dropdown")
        | {{ identity.currentUser.firstName + " " + identity.currentUser.lastName}}
        b.caret
      ul.dropdown-menu
        li(ng-show="identity.currentUser.isAdmin()")
          a.dropdown-item(href="/admin/users") Admin User
        li
          a.dropdown-item(href="", ng-click="signout()") Sign out